# Vercel Blob read-write token.
# Locally: create a Blob store in the Vercel dashboard (Storage tab) and copy
# the token, or run `vercel env pull .env.local` after linking the project.
# On Vercel: this is injected automatically when the Blob store is connected.
BLOB_READ_WRITE_TOKEN=
